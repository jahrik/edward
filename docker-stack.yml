version: '3.8'

services:
  edward:
    image: ghcr.io/jahrik/edward:latest
    environment:
      - OLLAMA_HOST=http://ollama:11434
    volumes:
      # Mount the local SQLite database into the container's working directory
      - ./data/edward_memory.db:/app/edward_memory.db
